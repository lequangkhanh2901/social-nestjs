export enum ConversationType {
  GROUP = 'GROUP',
  DUAL = 'DUAL',
}

export enum ConversationStatus {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
}

export enum MessageStatus {
  NORMAL = 'NORMAL',
  EDITED = 'EDITED',
  CANCEL = 'CANCEL',
}

export enum MessageViewSatus {
  SENT = 'SENT',
  RECEIVED = 'RECEIVED',
  VIEWED = 'VIEWED',
}

export enum ConversationRole {
  CHIEF = 'CHIEF',
  VICE_CHIEF = 'VICE_CHIEF',
  MEMBER = 'MEMBER',
}
